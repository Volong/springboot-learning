<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="github.io.volong.mapper.UserMapper" >

    <resultMap id="BaseResultMap" type="github.io.volong.entity.UserEntity" >
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="userName" property="userName" jdbcType="VARCHAR" />
        <result column="passWord" property="passWord" jdbcType="VARCHAR" />
        <result column="user_sex" property="userSex" javaType="github.io.volong.domain.UserSexEnum"/>
        <result column="nick_name" property="nickName" jdbcType="VARCHAR" />
    </resultMap>

    <!--定义一些通用的字段-->
    <sql id="Base_Column_List">
        id, userName, passWord, user_sex, nick_name
    </sql>

    <select id="getAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from users
    </select>

    <select id="getOne" parameterType="Long" resultMap="BaseResultMap">
        select
        <!--引入通用的 SQL 片段-->
        <include refid="Base_Column_List" />
        from users
        where id = #{id}
    </select>

    <insert id="insert" parameterType = "github.io.volong.entity.UserEntity">
        insert into users(userName, passWord, user_sex)
        values(#{userName}, #{passWord}, #{userSex})
    </insert>

    <update id="update" parameterType = "github.io.volong.entity.UserEntity">
        update users
            set
                <if test="userName != null">userName = #{userName},</if>
                <if test="passWord != null">passWord = #{passWord},</if>
                nick_name = #{nickName}
        where id = #{id}
    </update>

    <delete id="delete" parameterType = "Long">
        delete from users where id = #{id}
    </delete>
</mapper>